{
  "name": "cloudlaunch-api-worker",
  "version": "0.0.3",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "prepublish": "npm version patch --no-git-tag-version",
    "publish": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260516.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.92.0"
  }
}
