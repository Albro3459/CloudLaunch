# Frontend TODO

1. Keep the current UI compatible with the Lambda/API response shape until the rewrite starts.
2. Handle Firebase VPN statuses: `Pending`, `Running`, `Failed`, and `Terminated`.
3. Keep the VPN table visible for empty and loading states.
4. Preselect the single supported region while keeping the region dropdown in place.
5. Defer the full UI rewrite to a separate ticket; do not start it as part of OCI-over-HTTPS stabilization.
