# Frontend TODO

1. Handle Firebase VPN statuses: `Pending`, `Running`, `Failed`, and `Terminated`.
2. Keep the VPN table visible for empty and loading states.
3. Re-check the deploy, QR/config download, and admin terminate flows against the final Lambda URLs.
4. Defer the full UI rewrite to a separate ticket; keep this pass focused on OCI compatibility.
